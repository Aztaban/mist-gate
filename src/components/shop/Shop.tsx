import ProductList from './ProductList';

const Shop = () => {

  const content = (
    <main className="main-products">
      <ProductList />
    </main>
  );

  return content;
};

export default Shop;
